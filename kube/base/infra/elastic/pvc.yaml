apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elastic-data
  labels:
    app.kubernetes.io/name: elastic
    app.kubernetes.io/part-of: huly
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
