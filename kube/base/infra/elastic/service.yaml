apiVersion: v1
kind: Service
metadata:
  name: elastic
  labels:
    app.kubernetes.io/name: elastic
    app.kubernetes.io/part-of: huly
spec:
  selector:
    app.kubernetes.io/name: elastic
  ports:
    - name: http
      port: 9200
      targetPort: http
