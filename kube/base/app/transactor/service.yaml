apiVersion: v1
kind: Service
metadata:
  name: transactor
  labels:
    app.kubernetes.io/name: transactor
    app.kubernetes.io/part-of: huly
spec:
  selector:
    app.kubernetes.io/name: transactor
  ports:
    - name: ws
      port: 3333
      targetPort: ws
