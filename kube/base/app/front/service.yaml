apiVersion: v1
kind: Service
metadata:
  name: front
  labels:
    app.kubernetes.io/name: front
    app.kubernetes.io/part-of: huly
spec:
  selector:
    app.kubernetes.io/name: front
  ports:
    - name: http
      port: 8080
      targetPort: http
