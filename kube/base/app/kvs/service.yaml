apiVersion: v1
kind: Service
metadata:
  name: kvs
  labels:
    app.kubernetes.io/name: kvs
    app.kubernetes.io/part-of: huly
spec:
  selector:
    app.kubernetes.io/name: kvs
  ports:
    - name: http
      port: 8094
      targetPort: http
