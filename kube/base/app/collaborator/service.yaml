apiVersion: v1
kind: Service
metadata:
  name: collaborator
  labels:
    app.kubernetes.io/name: collaborator
    app.kubernetes.io/part-of: huly
spec:
  selector:
    app.kubernetes.io/name: collaborator
  ports:
    - name: ws
      port: 3078
      targetPort: ws
