apiVersion: v1
kind: Service
metadata:
  name: stats
  labels:
    app.kubernetes.io/name: stats
    app.kubernetes.io/part-of: huly
spec:
  selector:
    app.kubernetes.io/name: stats
  ports:
    - name: http
      port: 4900
      targetPort: http
