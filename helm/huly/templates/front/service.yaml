apiVersion: v1
kind: Service
metadata:
  name: front
  labels:
    {{- include "huly.labels" . | nindent 4 }}
    app: front
spec:
  selector:
    app: front
  ports:
    - name: http
      port: 8080
      targetPort: 8080
